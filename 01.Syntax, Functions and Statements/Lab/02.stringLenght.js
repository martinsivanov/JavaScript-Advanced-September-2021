function solve(first,second,third) {
    let sum = first.length + second.length + third.length
    let avarage = Math.floor(sum / 3)
    console.log(sum);
    console.log(avarage);
}
solve('chocolate', 'ice cream', 'cake')